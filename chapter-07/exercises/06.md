**Q**: If we allocate an array of longs using `calloc`, is the array initialized
to 0? If we allocate an array of pointers using `calloc`, is the array initialized
to null pointers?

---

**A**: Yes, and yes, because the memory `calloc` allocates is initialized to all
zero bits.
