**Q**: Some UNIX system implementations purposely arrange that, when a program
is executed, location 0 in the data segment is not accessible. Why?

---

**A**: Because location 0 is used to indicate that there is no data (NULL
pointer).
