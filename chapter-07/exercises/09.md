**Q**: In Section 7.7, why does the size of the executable file differ so
dramatically when we use shared libraries for such a trivial program?

---

**A**: When the program is built without using the shared libraries, but using
the static libraries, the library code is incorporated into the executable file.
