**Q**: In the output from the size command at the end of Section 7.6, why aren’t any sizes given
for the heap and the stack?

---

**A**: The size command reports the sizes of sections that are part of the
executable file. The heap and stack are runtime memory regions whose sizes are
determined during execution.
