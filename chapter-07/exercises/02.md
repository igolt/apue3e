**Q**: When is the output from the printfs in Figure 7.13 actually output?

---

**A**: If the `stdout` is referring to a iterative device each printf is writing
to the device. If `stdout` refers to a file, output occurs after the exit
handlers are executed.
