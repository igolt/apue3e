**Q**: If you open a file for read–write with the append flag, can you still
read from anywhere in the file using `lseek`? Can you use `lseek` to replace
existing data in the file? Write a program to verify this.

**A**: No, it's not possible to replace existing data in the file when the
O_APPEND flag is set. Every write will occur at the end of the file.
