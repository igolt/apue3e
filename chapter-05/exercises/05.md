**Q**: How would you use the `fsync` function (Section 3.13) with a standard I/O
stream?

---

**A**: `fsync` could be used on file flushing to ensure consistency of the file
system on disk with the contents of the buffer cache.
