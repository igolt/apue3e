**Q**: If the system uses a shadow file and we need to obtain the encrypted
password, how do we do so?

---

**A**: If a system uses a shadow file we can obtain the encrypted password by
reading and parsing the shadow password file or by using functions like
`getspnam` or `getspent`.
