**Q**: Some versions of the `finger(1)` command output "New mail received ..."
and "unread since ..." where ... are the corresponding times and dates. How can
the program determine these two times and dates?

---

**A**: These two times can be determined using the `stat` function, using the
modification time as the received time and access time as the last read time.
