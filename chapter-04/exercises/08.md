**Q**: When running the program in Figure 4.16, we check the available disk
space with the df(1) command. Why didn’t we use the du(1) command?

---

**A**: Because du is used to report file space usage, and does it recursively if
a directory is provided as an argument. df is the more appropriate command since
it reports file system space usage.
