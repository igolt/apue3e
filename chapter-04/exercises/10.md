**Q**: In Section 4.22, how does the system’s limit on the number of open files
affect the `myftw` function?

---

**A**: The system limit on the number of open files affect how deep we can
travel in the file system. Each time we go down in the directories hierarchy we
use `opendir` which open a new file descriptor.
